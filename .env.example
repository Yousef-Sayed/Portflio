# Environment Variables Template
# ===============================
# Copy this file to .env.local and fill in your values

# Convex Configuration (Required)
# Run 'npx convex dev' to automatically configure, OR fill manually
NEXT_PUBLIC_CONVEX_URL="https://your-convex-deployment.convex.cloud"

# For Convex CLI deployment (keep secret, never expose in NEXT_PUBLIC_)
CONVEX_DEPLOY_KEY=""

# Site URL (Required for SEO metadata)
# Set to your production domain for deployment
NEXT_PUBLIC_SITE_URL="https://your-domain.com"

# Optional: Analytics
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
